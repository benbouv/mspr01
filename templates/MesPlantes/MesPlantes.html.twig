{% extends 'base.html.twig' %}

{% block title %}Mes Plantes{% endblock %}

{% block body %}
<div class="container">
    <h1>Vos dernières plantes</h1>
    <div class="row flex">
        {% for plante in mesplantes %}
            <div class="col-3">
                <div class="card" style="width: auto;">
                    <a href="#" style="  color: black;
                        text-decoration: none;">
                        <img src="{{ asset('build/img/logo-cycloop.png') }}" class="card-img-top" alt="..." width="100" height="100">
                        <div class="card-body">
                            <h4 class="card-title" style="text-align: center;">{{ plante.Nom }}</h4>
                        </div>
                    </a>
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
